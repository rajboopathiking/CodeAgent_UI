import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"
import { ThemeProvider } from "@/components/theme-provider"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Thangarasu Samayal - Authentic South Indian Catering Service",
  description:
    "Traditional South Indian catering service specializing in authentic Tamil Nadu-style cooking for weddings, poojas, festivals, and special occasions.",
  keywords:
    "South Indian catering, Tamil Nadu cooking, traditional food, wedding catering, pooja catering, festival food, vegetarian catering",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>
        <ThemeProvider attribute="class" defaultTheme="light" enableSystem disableTransitionOnChange>
          {children}
        </ThemeProvider>
      </body>
    </html>
  )
}
